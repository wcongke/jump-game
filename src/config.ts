export module Config {
  export const tableHeight = 20 / 2;
  export const tableSize = 32 / 2;

  export const groundY = tableHeight;
  export const roleInitPosition = [-8, groundY, 25];
  export const gravity = 0.0001;
}
